import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    formContainer: {
        marginHorizontal: 30,
        marginTop: 40,
        marginBottom: 20,
    },

    input: {
        marginBottom: 20
    },

    buttonsContainer: {
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
        marginBottom: 30
    },

    button: {
        marginHorizontal: 12,
    },

    question: {
        marginTop: 30
    },

    questionsContainer: {
        marginBottom: 10
    }
});